<%= form_with(model: video, local: true) do |form| %>
  <% if video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(video.errors.count, "error") %> prohibited this video from being saved:</h2>

      <ul>
      <% video.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label>Youtube URL </label>
    <%= form.text_field :channel_id, id: :video_channel_id, class: "form-control", placeholder: "Enter Youtube URL" %>
  </div>

  
  <div class="form-group">
    <label>Video Title </label>
    <%= form.text_field :title, id: :video_title, class: "form-control", placeholder: "Enter Video Title"%>
  </div>

  
    <%= form.hidden_field :user_id, value: current_user.id %>


  <div class="actions">
    <%= form.submit class: "btn btn-block btn-auth shadow" %>
  </div>
<% end %>
